# Internshala Automation Environment Configuration
# Copy this file to .env and fill in your actual credentials

# Internshala Login Credentials (REQUIRED for full automation)
INTERNSHALA_EMAIL=your.email@example.com
INTERNSHALA_PASSWORD=your_password_here

# Browser Settings
HEADLESS=false
BROWSER_TIMEOUT=30000

# Export Settings  
CSV_OUTPUT_DIR=./exports
LOG_LEVEL=INFO

# Rate Limiting (to avoid being blocked)
REQUESTS_PER_MINUTE=30
CONCURRENT_REQUESTS=3

# Instructions:
# 1. Rename this file to .env (remove .template)
# 2. Replace the email and password with your actual Internshala credentials
# 3. Set HEADLESS=true if you want browser to run in background
# 4. Run: python cli.py login-test to verify credentials
